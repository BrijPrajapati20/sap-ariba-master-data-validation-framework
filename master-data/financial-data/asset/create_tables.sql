/*=============================================================
TABLE CREATION SCRIPT
Module : Asset Validation
=============================================================*/

CREATE TABLE itk_asset (

    company_code VARCHAR(20),

    asset_number VARCHAR(50),

    asset_description VARCHAR(255),

    asset_sub_number VARCHAR(50)

);

CREATE TABLE ariba_asset (

    company_code VARCHAR(20),

    asset_number VARCHAR(50),

    asset_description VARCHAR(255),

    asset_sub_number VARCHAR(50)

);
